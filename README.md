# NUMERA
Drop-in mathematical co-processor for LLMs. Pratt parser → 111-rule library → deterministic execution → verified JSON output. 9,046 lines of Rust, zero deps, sub-ms latency. Plug it into tool-use and never get 347×892 wrong again.
